{% extends 'base.html.twig' %}

{% block body %}
{% if users %}
<hr/>
    <h2 class="mb-3">Page User</h2>
    <div class="container">
    <div class="row">
    <div class="col-xs-8 col-sm-8 col-md-12 col-xl-12 table-responsive">
    <table class="table table-dark table-hover" align="center">
  <tr>
    <th>#</th>
    <th>email</th>
    <th>role</th>
    <th>date</th>
  </tr>
    {% for user in users %}
        {% if user.role == "ROLE_ADMIN" %}
    {% else %}
  <tr>
    <td>
    {{ user.id }}
    </td>
    <td>  
      {{ user.email }}
    </td>
    <td>  
      {{ user.role }}
    </td>
    <td>
      {{ user.created_at | date('d/m/Y') }}
    </td>
    <td>
<!-- Button trigger modal -->
<span data-bs-toggle="modal" data-bs-target="#exampleModal{{ user.id }}">
  <i class="fa-solid fa-trash text-danger"></i>
</span>
<!-- Modal -->
<div class="modal fade" id="exampleModal{{ user.id }}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body" style="color:black">
        <p>Ëtes - vous certain de vouloir faire cela ?</p>
        <small class="text-danger">Cette action sera irreversible !</small>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-warning" data-bs-dismiss="modal">Annuler</button>
   <a href="/public/user/remove/{{ user.id }}">
   <button type="button" class="btn btn-danger">Supprimer</button>
   </a>
      </div>
    </div>
  </div>
</div>
</td>
  </tr>
  {% endif %}
    {% endfor %}
</table>
    </div>
    </div>
    </div>
    {% endif %}
{% endblock %}